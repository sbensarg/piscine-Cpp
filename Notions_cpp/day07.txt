Templates
- model of how to build a function or a class with a particular type variable
example:

T ==> any type but systematically the same

template < typename T >
T const &	max(T const & x, T const & y) {
	return (x >=y ? x : y);
}

Default type

template < typename T = float >

main == > NomClass<>
